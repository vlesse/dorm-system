# 贡献指南

欢迎 Issue 和 PR。这份文档说明**怎么参与**和**什么样的改动会被合并**。

---

## 提 Issue

**报 Bug** 请带上：

- 复现步骤（用哪个演示账号、点了哪里）
- 期望结果 vs 实际结果
- 后端报错的话附上 `journalctl -u dorm-system` 或控制台输出
- 浏览器和版本（前端问题）

**提功能建议** 请说清楚**真实场景**：什么情况下会用到、现在是怎么绕过去的。
「加个 XX 功能」这样的描述很难判断该不该做、该怎么做。

海外园区的实际管理场景我们了解得越多，这个项目越有用 ——
如果你的园区有我们没考虑到的情况，非常欢迎开 Issue 讲讲。

---

## 提 PR

```bash
git checkout -b fix/短横线描述
# 改代码
npm run dev        # 自测
git commit -m "修复：退宿时未归还物品没有拦住"
```

- **一个 PR 做一件事**。混着改的 PR 审起来慢，也容易连带引入问题。
- **提交信息用中文，一句话说清做了什么。** 参考现有 `git log` 的风格。
- 改了 `schema.prisma` 的话，在 PR 描述里说明迁移影响。
- 改了接口的话，同步更新 [`docs/API.md`](docs/API.md)。

---

## 五条核心设计原则

**违反这五条的改动基本不会被合并。** 它们是整个系统的地基，
每一条背后都有具体的失败场景。

### 1. 最小可分配单位是床位，不是房间

想把分配逻辑简化成「按房间分配」的 PR 会被拒。
上下铺是两个独立位置，事故追溯要精确到铺位，空床数要准 —— 房间级建模做不到。

### 2. 入住记录永不覆盖

任何 `UPDATE occupancy SET bedId = ...` 形式的调宿实现都会被拒。
调宿必须是「关掉旧记录 + 新建新记录」。

历史一旦被覆盖，密接排查、事故追溯、按人天结算全部失效，而且**无法补救**。

### 3. 三个「人数」不能合并

`RoomType.defaultCapacity`（标称）、`Room.capacity`（核定）、`Bed` 行数（物理）
是三件不同的事。任何试图「简化」成一个字段的改动都会被拒。

「四人间实际住三人」在园区里是常态，不是数据错误。

### 4. 业务口径不写死在代码里

阈值、周期、分类、开关，全部应该落到 `SettingItem` 或字典表里。

写死一个数字之前先问自己：**这个值半年后会不会变？** 会变就进配置。
「证件到期提前 30 天提醒」这种就是典型的会变。

### 5. 权限和数据范围不能绕

- 每个写操作路由都要有 `requirePerm('模块:动作')`
- 每个列表 / 统计接口都要过 `buildingScope(req)`
- 操作人取自 `req.auth`，**永远不信前端传的 `operator` 字段**

漏掉 `buildingScope` 是这个项目最常见的一类 bug —— A 栋宿管会看到全园区数据。
**自测的时候用 `warden.a` 登录跑一遍**，任何显示了别栋数据的地方都是漏了。

---

## 代码风格

- **注释写「为什么」，不写「是什么」。**
  `// 遍历房间` 没有价值；`// 用核定人数不用物理床位数，降标房的空床数才准` 才有。
- **注释用中文** —— 使用方是中文团队。
- 跟着周围代码的写法走，不要单独引入新的模式或依赖。
- 新增依赖要在 PR 里说明为什么必须引入。
  认证部分**有意不用第三方库**（`node:crypto` 手写不到 80 行），不要「优化」成 jsonwebtoken。

---

## 特别欢迎的贡献

| | 说明 |
|---|---|
| **企业平台适配器** | 15 个平台占位里挑一个实现 `ChannelAdapter`，见 [开发文档](docs/DEVELOPMENT.md#接一个新的通知渠道)。有企业凭据能联调的话价值很大 |
| **费用结算模块** | 住宿费按人天、水电分摊、承包商月结。单价字段已经在存了 |
| **PostgreSQL 迁移脚本** | schema 已经 PG 兼容，缺的是数据搬迁脚本 |
| **英文 / Bahasa 文案校对** | 三语文案是机翻加人工核对的，母语者的修正很有价值 |
| **真实场景反馈** | 你的园区遇到过什么我们没考虑到的情况 |

---

## 开发环境

见 [开发文档](docs/DEVELOPMENT.md)。最短路径：

```bash
npm run setup && npm run dev
```

---

## 联系

lngsuan520@gmail.com
